apiVersion: v1
kind: Service
metadata:
  name: {{ include "httpd_hello_world.fullname" . }}
  labels:
    {{- include "httpd_hello_world.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "httpd_hello_world.selectorLabels" . | nindent 4 }}
