apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "httpd_hello_world.fullname" . }}-test-connection"
  labels:
    {{- include "httpd_hello_world.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "httpd_hello_world.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
